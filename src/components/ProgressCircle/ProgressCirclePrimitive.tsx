import {
  ProgressBar as RACProgressBar,
  type ProgressBarProps as RACProgressBarProps,
} from 'react-aria-components';

export type ProgressCirclePrimitiveProps = RACProgressBarProps;

export const ProgressCirclePrimitive = RACProgressBar;
